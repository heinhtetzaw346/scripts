# Keycloak Admin API data query

Please write a bash script to pull data from keycloak (rhbk)

Required DATA: users list and events.

Save both of them as csv files.

You can use the .env file. It contains USERNAME and PASSWORD of admin. (please do not look at it. just use it).

Make the url a variable and an input. Use https://nonprod-oneid.yomabank.org as default.
